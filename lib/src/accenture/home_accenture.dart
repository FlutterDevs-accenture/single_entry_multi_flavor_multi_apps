import 'package:flutter/material.dart';

class HomeAccenture extends StatelessWidget {
  const HomeAccenture({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Accenture Home')),
      body: Center(child: Text('Welcome to Accenture App')),
    );
  }
}
